apiVersion: apps/v1
kind: Deployment
metadata:
  name: osrs-tracker-web
  namespace: default
  labels:
    app: osrs-tracker-web
spec:
  replicas: 2
  revisionHistoryLimit: 1
  selector:
    matchLabels:
      app: osrs-tracker-web
  template:
    metadata:
      labels:
        app: osrs-tracker-web
    spec:
      imagePullSecrets:
        - name: regcred
      containers:
        - name: osrs-tracker-web
          image: freekmencke/osrs-tracker-web@sha256:74df09269c825432fdcc86a02f741971d11f7fa1ea896e45e0333197a21b8022
          imagePullPolicy: Always
          env:
            - name: HOST
              value: osrs-tracker.freekmencke.com
          ports:
            - containerPort: 8080
          readinessProbe:
            httpGet:
              path: /healthy
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /healthy
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: osrs-tracker-web-service
  namespace: default
spec:
  selector:
    app: osrs-tracker-web
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  type: ClusterIP
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: osrs-tracker-web-ingress
  namespace: default
  annotations:
    cert-manager.io/cluster-issuer: 'letsencrypt-prod'
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - osrs-tracker.freekmencke.com
      secretName: osrs-tracker-tls
  rules:
    - host: osrs-tracker.freekmencke.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: osrs-tracker-web-service
                port:
                  number: 80
